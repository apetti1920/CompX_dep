export type PointType = {
    x: number,
    y: number
}

export type DataType = {
    id: number,
    name: string,
    type: string[],
    pictureFile: string
}

export const DnDItemType = {
    LIBRARY_CARD: 'LIBRARY_CARD'
}
